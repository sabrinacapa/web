<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario de Altavoces</title>
</head>
<body>
    <h1>Formulario para Tienda de Altavoces</h1>
    <form action="procesar_altavoces.php" method="post">
        <!-- Selección de modelo -->
        <label for="modelo">Modelo:</label>
        <select id="modelo" name="modelo" required>
            <option value="R4">R4</option>
            <option value="R5">R5</option>
            <option value="SR1">SR1</option>
        </select>
        <br><br>

        <!-- Campo de potencia máxima -->
        <label for="potencia">Potencia máxima (50-100):</label>
        <input type="number" id="potencia" name="potencia" min="50" max="100" required>
        <br><br>

        <!-- Opción de Bluetooth -->
        <label for="bluetooth">¿Bluetooth?:</label>
        <input type="radio" id="bluetooth_si" name="bluetooth" value="Sí" required>
        <label for="bluetooth_si">Sí</label>
        <input type="radio" id="bluetooth_no" name="bluetooth" value="No" required>
        <label for="bluetooth_no">No</label>
        <br><br>

        <!-- Selección de color -->
        <label for="color">Color:</label>
        <input type="text" id="color" name="color" placeholder="Ingresa el color" required>
        <br><br>

        <!-- Botón de envío -->
        <button type="submit">Enviar</button>
    </form>
</body>
</html>
